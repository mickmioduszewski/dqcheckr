% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{resolve_col_type}
\alias{resolve_col_type}
\title{Resolve the effective type of a column, respecting config overrides}
\usage{
resolve_col_type(col, x, config)
}
\arguments{
\item{col}{Character. Column name.}

\item{x}{Character vector. The column's values (as read from the file).}

\item{config}{Named list. Merged configuration as returned by
\code{\link{load_config}}.}
}
\value{
A single character string: \code{"date"}, \code{"numeric"},
  \code{"character"}, or \code{"unknown"}.
}
\description{
Returns the type for \code{col} from the \code{column_types} map in
\code{config} if one is set, otherwise falls back to
\code{\link{infer_col_type}}. Use this in custom check scripts instead of
calling \code{infer_col_type()} directly so that type overrides are
respected.
}
\examples{
cfg_dir <- system.file("demonstrations/config", package = "dqcheckr")
cfg <- load_config("starwars_csv", config_dir = cfg_dir)
resolve_col_type("name", c("Luke", "Leia", "Han"), cfg)   # "character"

}
