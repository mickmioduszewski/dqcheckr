url: https://mickmioduszewski.github.io/dqcheckr/

template:
  bootstrap: 5

navbar:
  structure:
    left:  [intro, reference, articles, news]
    right: [github]

reference:
  - title: "Run checks"
    desc: "Main entry point and individual check runners"
    contents:
      - run_dq_check
      - run_qc_checks
      - run_comparison_checks
      - run_custom_checks
  - title: "Configuration & ingestion"
    desc: "Load config and read dataset files"
    contents:
      - load_config
      - detect_files
      - read_dataset
  - title: "Results"
    desc: "Construct and interpret result objects"
    contents:
      - dq_result
      - overall_status
      - infer_col_type
      - resolve_col_type
      - check_missing_rate
  - title: "Snapshot database & drift analysis"
    desc: "Query snapshot history and compare snapshots over time"
    contents:
      - read_recent_snapshots
      - list_snapshots
      - compare_snapshots
  - title: "Internals"
    desc: "Internal functions (not for direct use)"
    contents:
      - starts_with("check_")
      - starts_with("compare_")
      - infer_col_type
      - overall_status
      - compute_col_stats
      - init_snapshot_db
      - write_snapshot
      - render_report
      - starts_with("dot-")
