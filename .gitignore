# R build artefacts
*.Rproj.user
*.Rhistory
*.RData
*.Ruserdata
.Rproj.user/

# R check outputs
*.Rcheck/
*.tar.gz

# Data directories that dqcheckr may create at runtime (top-level only —
# anchored so they do NOT match inst/demonstrations/data/, a shipped fixture).
/data/
/reports/

# macOS
.DS_Store

# Compiled / installed
src/*.so
src/*.dll

# Pre-built vignette outputs and internal build state
build/
inst/doc/
/doc/
/Meta/
tests/testthat/_problems/
