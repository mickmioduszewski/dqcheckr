# Render the HTML data quality report

Render the HTML data quality report

## Usage

``` r
render_report(
  dataset_name,
  file_name,
  file_path,
  df,
  qc_results,
  cp_results,
  custom_results,
  snapshot_history,
  config,
  output_dir,
  open_report = TRUE
)
```
