# Write a run snapshot to the SQLite database

Write a run snapshot to the SQLite database

## Usage

``` r
write_snapshot(
  db_path,
  dataset_name,
  file_name,
  df,
  qc_results,
  cp_results,
  custom_results,
  config,
  col_stats = NULL
)
```
